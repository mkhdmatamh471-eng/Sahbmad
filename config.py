import re
BOT_TOKEN = "8498451295:AAGt1R7THllSjYtEe5hvIEPnPhRkS_iBcnU"

# --- 2. إعدادات الأحياء (المدينة المنورة) ---
# تم ترتيبها وتنظيفها لضمان مطابقة أدق
CITIES_DISTRICTS = {
    "المدينة المنورة": [
        # --- الأحياء السكنية (ترتيب أبجدي تقريبي) ---
        "أبو مرخة", "أبيار علي", "الأسواف", "البخاري", "الإسكان", "البحر", 
        "البدراني", "الجمعة", "الجرف", "الحزام", "الحمراء", "الخاتم", "الخالدية", 
        "الدائرة", "الدائري", "الدويخله", "الدويمة", "الدعيثة", "الرانونا", "الرناونة", 
        "الربوة", "السيح", "الشروق", "الشرق", "الشهداء", "الشيبية", "الظاهرة", 
        "العاقول", "العالية", "العوالي", "العريض", "العزيزية", "العصبة", 
        "العنابس", "العنبرية", "العيون", "الفتح", "القبلتين", "القارات", "المبعوث", 
        "المطار", "المغيسله", "الملك فهد", "الملك فيصل", "المناخة", "المصانع", 
        "النور", "النبلاء", "النقيع", "النقمي", "الهجرة", "الواحة", "الوادي", 
        "بئر الماشي", "بئر عثمان", "باقدو", "بني حارثة", "بني ظفر", "تلال علي", 
        "تلال", "حمراء الأسد", "دفاع", "ذو الحليفة", "رهط", "سيد الشهداء", 
        "سلطانة", "سكة الحديد", "السقيا", "شظاة", "شوران", "عروة", "قباء", 
        "قربان", "مخطط التلال", "مهزور", "وعيرة", "العاقول", "الغابة", "المصانع",
        "العصيبي", "تلعة الهبوب", "الملك فهد", "العيون", "الزهراء", "الفيحاء",
        "المنار", "سكن الحرس", "السديري", "الدويمة", "المطار القديم",

        # --- المعالم والمساجد الكبرى ---
        "الحرم", "المسجد النبوي", "ساحات الحرم", "مسجد قباء", "مسجد القبلتين", 
        "مسجد الميقات", "آبار علي", "السبع مساجد", "جبل أحد", "مقبرة البقيع",

        # --- النقل والمواصلات ---
        "محطة القطار", "قطار الحرمين", "مطار المدينة", "مطار محمد بن عبدالعزيز",
        "النقل الجماعي", "موقف مكة", "موقف جدة", "طريق الهجرة", "طريق الجامعات",

        # --- الجامعات والتعليم ---
        "جامعة طيبة", "الجامعة الإسلامية", "كلية التقنية", "جامعة الأمير مقرن", 
        "كليات الريان", "إدارة التعليم",

        # --- المستشفيات ---
        "المستشفى الألماني", "الحرس الوطني", "مستشفى الملك سلمان", "مستشفى الملك فهد", 
        "المواساة", "مستشفى الولادة", "مستشفى ممار", "مستشفى أحد", "مستشفى المدينة الوطني",
        "مستشفى الدار", "مستشفى الأمل", "مستشفى الصدرية",

        # --- المولات والأسواق ---
        "النور مول", "الراشد مول", "العالية مول", "منار مول", "سوق بلال", "سوق الغنم",
        "سوق الدخيل", "سوق الخضار", "بندة", "العثيم", "الدانوب", "سوق المدينة الدولي",

        # --- الفنادق والمناطق الحيوية ---
        "المركزية", "المنطقة المركزية", "المركزية الشمالية", "المركزية الجنوبية", 
        "ممشى الهجرة", "ممشى العباس", "حديقة الملك فهد", "البيضاء", "متنزه البيضاء"
    ]
}

# --- 3. معالجة النصوص (الذكاء اللغوي) ---

try:
    # إنشاء مجمع يتسع لـ 1 إلى 20 اتصال متزامن
    db_pool = psycopg2.pool.ThreadedConnectionPool(
        minconn=1, 
        maxconn=20, 
        dsn=DB_URL,
        sslmode='require' # ضروري جداً لريندر
    )
    print("✅ تم تفعيل مجمع اتصالات قاعدة البيانات (Connection Pool)")
except Exception as e:
    print(f"❌ فشل إنشاء مجمع الاتصالات: {e}")
    db_pool = None

# ==========================================
# 2. دوال الإدارة الأساسية (Helper Functions)
# ==========================================

def get_db_connection():
    """الحصول على اتصال آمن مع فحص حيويته (Health Check)"""
    global db_pool
    
    if db_pool is None:
        try:
            fixed_url = DB_URL.replace("postgres://", "postgresql://")
            db_pool = psycopg2.pool.ThreadedConnectionPool(1, 20, dsn=fixed_url, sslmode='require')
        except Exception as e:
            print(f"❌ فشل إنشاء المجمع: {e}")
            return None

    try:
        conn = db_pool.getconn()
        
        # --- الفحص الحيوي (Health Check) ---
        # نقوم بعمل استعلام فارغ للتأكد من أن الاتصال لا يزال حياً
        try:
            with conn.cursor() as tmp_cur:
                tmp_cur.execute('SELECT 1')
        except (psycopg2.OperationalError, psycopg2.InterfaceError):
            print("🔄 اكتشاف اتصال ميت في المجمع، يتم استبداله...")
            db_pool.putconn(conn, close=True) # التخلص من الاتصال التالف
            return db_pool.getconn()          # جلب اتصال جديد ونظيف
            
        return conn
    except Exception as e:
        print(f"⚠️ خطأ في سحب اتصال: {e}")
        db_pool = None 
        return None

def release_db_connection(conn):
    if not conn:
        return
    try:
        if db_pool:
            # التحقق إذا كان الاتصال لا يزال مفتوحاً قبل إعادته للمجمع
            if not conn.closed:
                db_pool.putconn(conn)
            else:
                # إذا كان مغلقاً أصلاً، فقط نتجاهله (المجمع سيتولى التعويض)
                pass
        else:
            conn.close()
    except Exception as e:
        print(f"⚠️ خطأ أثناء تحرير الاتصال: {e}")
        try: conn.close() 
        except: pass


def normalize_text(text):
    """تنظيف وتوحيد النصوص العربية لزيادة دقة البحث عن الأحياء والطلبات"""
    if not text: return ""

    # 1. تحويل للصغير وإزالة المسافات الزائدة
    text = text.strip().lower()

    # 2. إزالة التشكيل (فتحة، ضمة، إلخ)
    text = re.sub(r"[\u064B-\u0652]", "", text)

    # 3. توحيد الحروف المتشابهة
    replacements = {
        "أ": "ا", "إ": "ا", "آ": "ا",
        "ة": "ه", "ى": "ي", "ئ": "ي", "ؤ": "و"
    }
    for old, new in replacements.items():
        text = text.replace(old, new)

    # 4. إزالة تكرار الحروف (مشوااااار -> مشوار)
    text = re.sub(r'(.)\1+', r'\1', text)

    # 5. معالجة الـ التعريف لزيادة مرونة البحث
    # نقوم بإزالة "ال" إذا كانت تجعل الكلمة أكثر من 3 حروف (مثل العزيزية -> عزيزيه)
    words = text.split()
    clean_words = []
    for w in words:
        if w.startswith("ال") and len(w) > 3:
            clean_words.append(w[2:])
        else:
            clean_words.append(w)

    return " ".join(clean_words)